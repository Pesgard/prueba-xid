# AWS Configuration
AWS_REGION=us-east-1
AWS_PROFILE=default

# S3 Buckets (automatically set by Serverless Framework)
UPLOADS_BUCKET=report-processor-api-uploads-dev
RESULTS_BUCKET=report-processor-api-results-dev

# Serverless Stage
STAGE=dev 